# MEV-X Homelander Docs (Docusaurus)

## Replace placeholders (required)
Edit `docusaurus.config.js` and replace:
- `USERNAME` → your GitHub username (or org)
- `REPO` → your repo name

## Enable Pages
Repo → Settings → Pages → Source: **GitHub Actions**

## Deploy
Push to `main`. GitHub Actions builds & deploys automatically.
